name: Deploy to github pages
on:
    push:
      branches:
        - master
jobs:
    gh-pages-deploy:
      name: Deploying to gh-pages
      runs-on: ubuntu-latest
      steps:
        - name: Setup Node.js for use with actions
          uses: actions/setup-node@v2
          with:
            version:  12.x

        - name: Checkout branch
          uses: actions/checkout@v2
#
#        - name: Upgrade NPM
#          run: npm install -g npm

        - name: Clean install dependencies
          run: npm ci

        - name: Run deploy script
          run: |
            git config user.name "suchomelova-dana" && git config user.email "danka.suchomelova@seznam.cz"
            npm run gh-pages-deploy
          clean-exclude: |
            CNAME




#name: Build Vue
#on:
#  push:
#    branches:
#      - master
#  workflow_dispatch:
#jobs:
#  build_deploy_vue:
#    runs-on: ubuntu-latest
#    permissions:
#      contents: read
#      pull-requests: write
#    name: Build and Deploy Vue
#    steps:
#      - uses: actions/checkout@v2
#
#      - id: Build-And-Deploy
#        run: |
#            git config --global user.email "danka.suchomelova@seznam.cz"
#            git config --global user.name "suchomelova-dana"
#            npm run deploy




#    steps:
#      - uses: actions/checkout@v2
#      - id: Build-Deploy-Vue
#        uses: thefrustrateddev/vue-deploy-github-pages@v1.0.0
#        with:
#          username: 'suchomelova-dana'
#          reponame: 'ekos-zitenice'
#          token: ${{ secrets.GITHUB_TOKEN }} # no need to change this line this is used to create the branch in your application repository
#
#      - id: Create-CNAME
#        run: echo 'ekos-zitenice.cz' > CNAME






#name: Build Vue
#on:
#  push:
#    branches:
#      - master
#  workflow_dispatch:
#
#jobs:
#  build_vue:
#    runs-on: ubuntu-latest
#    name: Build Vue
#    steps:
#      - uses: actions/checkout@v2
#
#      - id: Build-Vue
#        uses: xRealNeon/VuePagesAction@1.0.0
#        with:
#          username: 'suchomelova-dana'
#          reponame: 'ekos-zitenice'
#          token: ${{ secrets.GITHUB_TOKEN }}


